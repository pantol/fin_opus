# Telegram alerts (optional). Without a token, alerts run in dry-run mode (printed to console).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Phase 2+ only (LLM is NOT used in Phases 0+1 / money path).
# OPENROUTER_API_KEY=
